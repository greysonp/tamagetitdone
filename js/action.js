this.tgd = this.tgd || {};

(function()
{
    // Namespace var
    var action = {};

    action.test = function()
    {
        
    }

    // Copy back
    this.tgd.action = action;
})();